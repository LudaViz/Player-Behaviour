# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Pages
        uses: actions/configure-pages@v6

      # Create a staging folder and copy ONLY the workshop into it
      - name: Prepare Deployment Folder
        run: |
          mkdir _site
          cp -r "./Post-Mortem Workshop" ./_site/

      # Fix permissions to prevent 403/404 errors during upload
      - name: Fix permissions
        run: chmod -R +rX ./_site

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          # Upload the staging folder, NOT the root repository
          path: './_site'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
